// Канал змін ролей -- НАШ, під нашим іменем мода.
//
// Був у ядрі (OZ_Rpc.RPC_ROLE_REQ/RPC_ROLE_RES, RegisterRoles, RoleRequest,
// RoleRespond) і поїхав сюди 2026-09-04. Ядро тримало реєстрацію й обидва
// кінці заради гри, якої в ньому немає: без цього мода той RPC не слухав
// ніхто, і половина конверта стояла мертвою.
//
// CF ключує RPC парою (ім'я мода, ім'я функції), тому пара
// ("OpenZone_Factions", "OZF_RoleReq") не може зіткнутись ні з ядровою, ні з
// чужою: збігтись мусили б ОБИДВА рядки.
//
// ЩО ТУТ НЕ ПОВТОРЮЄТЬСЯ З ЯДРА: різання довгого тіла на частини. Ядро ріже
// сторінкові й адмінські конверти, бо ті возять JSON у кілька кілобайт, а
// рушійний RPC псує рядки понад ~1024 байти (OZ_Const.RPC_STR_CHUNK, 900).
// Тут не возиться JSON узагалі -- операція, адреса цілі й один аргумент, --
// і жодне з трьох полів не наближається до межі. Єдине, що може вирости, --
// це `why` у відповіді, коли причину дає міст своїми словами: воно не ріжеться
// на частини, а ПІДРІЗАЄТЬСЯ на тому ж порозі; чому саме так -- нижче.
//
// ЗВОРОТНОГО КАНАЛУ В НАС БІЛЬШЕ НЕМАЄ (2026-09-06). Був "OZF_RoleRes" --
// Param3<string,bool,string> до клієнта, приймач-одинак і modded
// MissionGameplay, щоб його зареєструвати, -- і все це віддавало почуте в
// OZ_Notice.Take, тобто в ядровий збірник відповідей. Ядровий канал
// "OZ_Notice" возить рівно той самий конверт до того самого збірника й
// реєструється завжди (OZ_MissionGameplay), тож другий був точною копією
// першого разом із двома файлами навколо. Підрізання лишається тут, бо
// довгий `why` буває тільки в нас: у ядра причини -- ключі таблиці рядків.

class OZF_Rpc
{
    // ІМ'Я -- ВЛАСНЕ, з префіксом мода. CF ключує RPC парою (ім'я мода, ім'я
    // функції), тож ("OpenZone_Factions", "OZF_RoleReq") не зіткнеться ні з
    // ядровою, ні з чужою: збігтись мусили б ОБИДВА рядки.
    //
    // Зареєстрована функція МУСИТЬ мати рівно цю форму -- її задає диспетчер
    // CF (Param4 + CallFunctionParams), ніде не оголошуючи явно:
    //
    //   void Ім'я(CallType type, ParamsReadContext ctx, PlayerIdentity sender, Object target)
    //
    // Чотири параметри в цьому порядку, void, ім'я збігається з рядком
    // посимвольно, метод НЕ статичний.
    static const string RPC_ROLE_REQ = "OZF_RoleReq";

    // Кого міняємо -- ІМ'ЯМ, а не uid.
    //
    // Чужого Steam64 клієнт не бачить НІКОЛИ -- це межа, яку тримає вся
    // сторінка контактів, і вона не робиться винятком заради зручності. Кому
    // належить ім'я, вирішує сервер, і серед кого шукати -- теж він.
    //
    // Актора не називаємо взагалі: він завжди береться з sender.
    //
    // guaranteed за замовчуванням FALSE. Усе, що тут надсилається, має
    // значення, тому true передається явно скрізь.
    static void RoleRequest(string op, string targetName, string arg)
    {
        Param3<string, string, string> p =
            new Param3<string, string, string>(op, targetName, arg);
        GetRPCManager().SendRPC(OZF_Const.MOD, RPC_ROLE_REQ, p, true);
    }

    // `why` -- або ключ таблиці рядків (STR_OZ_...), або ГОТОВИЙ ТЕКСТ від
    // моста. Друге тому, що причину відмови Discord знає лише він, і «бот не
    // може керувати цією роллю» набагато корисніше за наш код помилки. Той,
    // хто малює, розрізняє їх за префіксом (OZ_Notice.Text).
    //
    // ТЕКСТ МОСТА -- ЄДИНЕ ТУТ, ЩО МОЖЕ ВИРОСТИ, І ПІДРІЗАЄМО ЙОГО МИ.
    //
    // Тут стояло «міст кліпає свої рядки сам» -- неправда: clip.js кличуть
    // discord.js, index.js і news.js, а roles.js віддає причину відмови як є,
    // включно із сирим текстом помилки Discord. Що справді її обмежує -- це
    // розбір JSON у рушії: JsonFileLoader ріже КОЖНЕ рядкове значення на 1023
    // байтах (зміряно 2026-08-28, скіл persistence-networking), тобто `why`
    // доїжджає сюди рівно НА тій межі, за якою рушійний RPC починає віддавати
    // "String CORRUPTED", а не під нею -- і поруч у тому ж конверті їде `op`.
    //
    // Тож ріжемо самі, на тому ж порозі, що й довгі тіла ядра, і різаком ядра
    // -- по межі символу, бо Length()/Substring() байтові, а текст UTF-8.
    //
    // `op` РІЖЕТЬСЯ ТАК САМО. Він приходить із клієнтського RPC
    // (OZF_Module, data.param1) і повертається сюди недоторканим: клієнт із
    // п'ятикілобайтною операцією псував собі ж повідомлення у відповідь.
    // Шкода тільки своя, але ціна перевірки -- один рядок, а «поле з
    // проводу, яке ми не міряли» не мусить існувати взагалі.
    static void RoleRespond(PlayerIdentity to, string op, bool ok, string why)
    {
        OZ_Rpc.Notice(to, Clip(op), ok, Clip(why));
    }

    private static string Clip(string s)
    {
        if (s.Length() <= OZ_Const.RPC_STR_CHUNK)
            return s;
        return s.Substring(0, OZ_Rpc.CutSafe(s, 0, OZ_Const.RPC_STR_CHUNK));
    }
}
