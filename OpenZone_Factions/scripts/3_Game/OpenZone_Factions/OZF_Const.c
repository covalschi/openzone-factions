// Константи мода фракцій.
//
// ЖИВУТЬ У 3_Game, а не поруч з адмінською сторінкою, як було до 2026-09-04.
// Причина технічна й жорстка: на них дивиться і 4_World (модуль, операції
// ролей), і 5_Mission обох склейок (сторінка КПК, панель VPP), а видимості
// знизу вгору в Enforce немає -- порядок компіляції 3_Game -> 4_World ->
// 5_Mission і тільки він.

class OZF_Const
{
    // ІМ'Я МОДА ДЛЯ CF RPCManager -- ВЛАСНЕ, не ядрове.
    //
    // CF ключує RPC парою (ім'я мода, ім'я функції), тож зіткнення можливе
    // лише при збігу ОБОХ рядків. Поки канал ролей жив у ядрі, він їхав під
    // іменем "OpenZone" -- тобто ядро тримало реєстрацію заради гри, якої в
    // ньому немає. Власне ім'я знімає це остаточно: сервер без цього мода не
    // має ні відправника, ні приймача, і прохання змінити роль просто нікуди
    // не йде.
    static const string MOD = "OpenZone_Factions";

    // Ім'я АДМІНСЬКОГО РОЗДІЛУ, не сторінки. Рядок той самий, місце інше:
    // розділи живуть у OZ_AdminRegistry і їдуть конвертом OZ_AdminReq.
    static const string SECTION = "factions";

    // ВЛАСНИЙ ФАЙЛ НАЛАШТУВАНЬ, поруч із рештою файлів серії.
    //
    // Був розділом "Faction" у конфігу ядра; поїхав сюди 2026-09-04 разом з
    // усім іншим фракційним. Ім'я в тому ж стилі, що й OZ_Factions_Loadouts
    // .json та OZ_Radio_Settings.json: адмін бачить у теці, чий файл чий.
    static const string SETTINGS = "$profile:OpenZone\\OZ_Factions_Settings.json";
}
