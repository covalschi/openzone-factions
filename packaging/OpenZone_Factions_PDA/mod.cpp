// How this mod presents itself in the launcher and the in-game mod list.
// Without it DayZ shows the folder name and nothing else. The picture and
// logo paths are left out on purpose: DayZ draws a blank for a missing
// texture and says nothing, so a wrong path is worse than none.

name        = "OpenZone Factions PDA";
author      = "Zone Protocol";
authorID    = "";
version     = "0.1";
description = "Companion mod. The faction tab inside the OpenZone PDA. REQUIRES OpenZone Core, OpenZone Factions and OpenZone PDA -- without all three the game refuses to load. github.com/covalschi/openzone-factions";
tooltip     = "OpenZone Factions PDA";
overview    = "The faction tab of the OpenZone PDA. Install only alongside OpenZone Core, OpenZone Factions and OpenZone PDA.";
