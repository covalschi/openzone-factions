// How this mod presents itself in the launcher and the in-game mod list.
// Without it DayZ shows the folder name and nothing else. The picture and
// logo paths are left out on purpose: DayZ draws a blank for a missing
// texture and says nothing, so a wrong path is worse than none.

name        = "OpenZone Factions VPP";
author      = "Zone Protocol";
authorID    = "";
version     = "0.1";
description = "Companion mod. The factions pane inside the OpenZone admin tab of VPP Admin Tools. REQUIRES OpenZone Core, OpenZone Factions, OpenZone Core VPP and VPP Admin Tools -- without all four the game refuses to load. github.com/covalschi/openzone-factions";
tooltip     = "OpenZone Factions VPP";
overview    = "The factions admin pane for VPP Admin Tools. Install only alongside OpenZone Core, OpenZone Factions, OpenZone Core VPP and VPP Admin Tools.";
